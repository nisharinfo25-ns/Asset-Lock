{
  "name": "asset-lock-workspace",
  "version": "1.0.0",
  "description": "Blockchain Based Decentralised Identity & Access Control for Secure Digital Asset Management",
  "private": true,
  "scripts": {
    "dev": "npm --prefix frontend run dev",
    "backend": "npm --prefix backend run backend",
    "build:frontend": "npm --prefix frontend run build",
    "compile:contract": "npm --prefix blockchain run compile",
    "test:backend": "npm --prefix backend test",
    "test": "npm --prefix backend test"
  }
}
